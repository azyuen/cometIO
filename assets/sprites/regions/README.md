# Region sprites

Upload the 16 generated 64×64 PNG files from `cometIO_region_sprites_64px.zip` into this folder without renaming them.

The game loads these files automatically through `comet-region-sprites-v1.js` and displays them at 128×128 with nearest-neighbour filtering for crisp pixel art.
